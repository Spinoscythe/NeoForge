--- a/net/minecraft/server/packs/AbstractPackResources.java
+++ b/net/minecraft/server/packs/AbstractPackResources.java
@@ -32,7 +_,7 @@
         } else {
             Object object;
             try (InputStream inputstream = iosupplier.get()) {
-                object = getMetadataFromStream(p_389404_, inputstream, this.location);
+                object = getMetadataFromStream(p_389404_, inputstream);
             }
 
             return (T)object;
@@ -40,12 +_,12 @@
     }
 
     @Nullable
-    public static <T> T getMetadataFromStream(MetadataSectionType<T> p_389726_, InputStream p_10216_, PackLocationInfo p_434958_) {
+    public static <T> T getMetadataFromStream(MetadataSectionType<T> p_389726_, InputStream p_10216_) {
         JsonObject jsonobject;
         try (BufferedReader bufferedreader = new BufferedReader(new InputStreamReader(p_10216_, StandardCharsets.UTF_8))) {
             jsonobject = GsonHelper.parse(bufferedreader);
         } catch (Exception exception) {
-            LOGGER.error("Couldn't load {} {} metadata: {}", p_434958_.id(), p_389726_.name(), exception.getMessage());
+            LOGGER.error("Couldn't load {} metadata", p_389726_.name(), exception);
             return null;
         }
 
@@ -53,7 +_,7 @@
             ? null
             : p_389726_.codec()
                 .parse(JsonOps.INSTANCE, jsonobject.get(p_389726_.name()))
-                .ifError(p_432469_ -> LOGGER.error("Couldn't load {} {} metadata: {}", p_434958_.id(), p_389726_.name(), p_432469_.message()))
+                .ifError(p_389537_ -> LOGGER.error("Couldn't load {} metadata: {}", p_389726_.name(), p_389537_))
                 .result()
                 .orElse(null);
     }
@@ -61,5 +_,10 @@
     @Override
     public PackLocationInfo location() {
         return this.location;
+    }
+
+    @Override
+    public String toString() {
+        return String.format(java.util.Locale.ROOT, "%s: %s", getClass().getName(), location.id());
     }
 }
