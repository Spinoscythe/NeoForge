--- a/net/minecraft/world/level/block/entity/ConduitBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/ConduitBlockEntity.java
@@ -83,7 +_,7 @@
             updateHunting(p_155407_, list);
         }
 
-        LivingEntity livingentity = EntityReference.getLivingEntity(p_155407_.destroyTarget, p_155404_);
+        LivingEntity livingentity = EntityReference.get(p_155407_.destroyTarget, p_155404_, LivingEntity.class);
         animationTick(p_155404_, p_155405_, list, livingentity, p_155407_.tickCount);
         if (p_155407_.isActive()) {
             p_155407_.activeRotation++;
@@ -149,10 +_,8 @@
                         BlockPos blockpos1 = p_155416_.offset(j1, k1, l1);
                         BlockState blockstate = p_155415_.getBlockState(blockpos1);
 
-                        for (Block block : VALID_BLOCKS) {
-                            if (blockstate.is(block)) {
-                                p_155417_.add(blockpos1);
-                            }
+                        if (blockstate.isConduitFrame(p_155415_, blockpos1, p_155416_)) {
+                            p_155417_.add(blockpos1);
                         }
                     }
                 }
@@ -181,7 +_,7 @@
 
     private static void updateAndAttackTarget(ServerLevel p_423464_, BlockPos p_423617_, BlockState p_423636_, ConduitBlockEntity p_423533_, boolean p_423687_) {
         EntityReference<LivingEntity> entityreference = updateDestroyTarget(p_423533_.destroyTarget, p_423464_, p_423617_, p_423687_);
-        LivingEntity livingentity = EntityReference.getLivingEntity(entityreference, p_423464_);
+        LivingEntity livingentity = EntityReference.get(entityreference, p_423464_, LivingEntity.class);
         if (livingentity != null) {
             p_423464_.playSound(
                 null, livingentity.getX(), livingentity.getY(), livingentity.getZ(), SoundEvents.CONDUIT_ATTACK_TARGET, SoundSource.BLOCKS, 1.0F, 1.0F
@@ -204,7 +_,7 @@
         } else if (p_423553_ == null) {
             return selectNewTarget(p_423665_, p_155410_);
         } else {
-            LivingEntity livingentity = EntityReference.getLivingEntity(p_423553_, p_423665_);
+            LivingEntity livingentity = EntityReference.get(p_423553_, p_423665_, LivingEntity.class);
             return livingentity != null && livingentity.isAlive() && p_155410_.closerThan(livingentity.blockPosition(), 8.0) ? p_423553_ : null;
         }
     }
@@ -214,7 +_,7 @@
         List<LivingEntity> list = p_423488_.getEntitiesOfClass(
             LivingEntity.class, getDestroyRangeAABB(p_423513_), p_427192_ -> p_427192_ instanceof Enemy && p_427192_.isInWaterOrRain()
         );
-        return list.isEmpty() ? null : EntityReference.of(Util.getRandom(list, p_423488_.random));
+        return list.isEmpty() ? null : new EntityReference<>(Util.getRandom(list, p_423488_.random));
     }
 
     private static AABB getDestroyRangeAABB(BlockPos p_155432_) {
