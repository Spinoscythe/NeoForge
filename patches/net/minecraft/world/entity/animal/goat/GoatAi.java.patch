--- a/net/minecraft/world/entity/animal/goat/GoatAi.java
+++ b/net/minecraft/world/entity/animal/goat/GoatAi.java
@@ -53,9 +_,9 @@
     private static final UniformInt TIME_BETWEEN_RAMS_SCREAMER = UniformInt.of(100, 300);
     private static final TargetingConditions RAM_TARGET_CONDITIONS = TargetingConditions.forCombat()
         .selector(
-            (p_432548_, p_432549_) -> !p_432548_.getType().equals(EntityType.GOAT)
-                && (p_432549_.getGameRules().getBoolean(GameRules.RULE_MOBGRIEFING) || !p_432548_.getType().equals(EntityType.ARMOR_STAND))
-                && p_432549_.getWorldBorder().isWithinBounds(p_432548_.getBoundingBox())
+            (p_427091_, p_427092_) -> !p_427091_.getType().equals(EntityType.GOAT)
+                && (p_427092_.getGameRules().getBoolean(GameRules.RULE_MOBGRIEFING) || !p_427091_.getType().equals(EntityType.ARMOR_STAND))
+                && p_427092_.getWorldBorder().isWithinBounds(p_427091_.getBoundingBox())
         );
     private static final float SPEED_MULTIPLIER_WHEN_RAMMING = 3.0F;
     public static final int RAM_MIN_DISTANCE = 4;
