--- a/net/minecraft/world/entity/monster/piglin/PiglinAi.java
+++ b/net/minecraft/world/entity/monster/piglin/PiglinAi.java
@@ -154,7 +_,7 @@
             10,
             ImmutableList.of(
                 SetEntityLookTarget.create(PiglinAi::isPlayerHoldingLovedItem, 14.0F),
-                StartAttacking.<Piglin>create((p_432552_, p_432553_) -> p_432553_.isAdult(), PiglinAi::findNearestValidAttackTarget),
+                StartAttacking.<Piglin>create((p_427117_, p_427118_) -> p_427118_.isAdult(), PiglinAi::findNearestValidAttackTarget),
                 BehaviorBuilder.triggerIf(Piglin::canHunt, StartHuntingHoglin.create()),
                 avoidRepellent(),
                 babySometimesRideBabyHoglin(),
@@ -189,7 +_,7 @@
             ImmutableList.<net.minecraft.world.entity.ai.behavior.BehaviorControl<? super Piglin>>of(
                 avoidRepellent(),
                 SetEntityLookTarget.create(PiglinAi::isPlayerHoldingLovedItem, 14.0F),
-                StartAttacking.<Piglin>create((p_432554_, p_432555_) -> p_432555_.isAdult(), PiglinAi::findNearestValidAttackTarget),
+                StartAttacking.<Piglin>create((p_427115_, p_427116_) -> p_427116_.isAdult(), PiglinAi::findNearestValidAttackTarget),
                 BehaviorBuilder.<Piglin>triggerIf(p_34804_ -> !p_34804_.isDancing(), GoToTargetLocation.create(MemoryModuleType.CELEBRATE_LOCATION, 2, 1.0F)),
                 BehaviorBuilder.<Piglin>triggerIf(Piglin::isDancing, GoToTargetLocation.create(MemoryModuleType.CELEBRATE_LOCATION, 4, 0.6F)),
                 new RunOne<Piglin>(
@@ -377,7 +_,7 @@
         ItemStack itemstack = p_34868_.getItemInHand(InteractionHand.OFF_HAND);
         p_34868_.setItemInHand(InteractionHand.OFF_HAND, ItemStack.EMPTY);
         if (p_34868_.isAdult()) {
-            boolean flag = isBarterCurrency(itemstack);
+            boolean flag = itemstack.isPiglinCurrency();
             if (p_34869_ && flag) {
                 throwItems(p_34868_, getBarterResponseItems(p_34868_));
             } else if (!flag) {
@@ -461,7 +_,7 @@
             return false;
         } else if (isAdmiringDisabled(p_34858_) && p_34858_.getBrain().hasMemoryValue(MemoryModuleType.ATTACK_TARGET)) {
             return false;
-        } else if (isBarterCurrency(p_34859_)) {
+        } else if (p_34859_.isPiglinCurrency()) {
             return isNotHoldingLovedItemInOffHand(p_34858_);
         } else {
             boolean flag = p_34858_.canAddToInventory(p_34859_);
@@ -551,7 +_,7 @@
     }
 
     protected static boolean canAdmire(Piglin p_34910_, ItemStack p_34911_) {
-        return !isAdmiringDisabled(p_34910_) && !isAdmiringItem(p_34910_) && p_34910_.isAdult() && isBarterCurrency(p_34911_);
+        return !isAdmiringDisabled(p_34910_) && !isAdmiringItem(p_34910_) && p_34910_.isAdult() && p_34911_.isPiglinCurrency();
     }
 
     protected static void wasHurtBy(ServerLevel p_376829_, Piglin p_34838_, LivingEntity p_34839_) {
@@ -640,7 +_,7 @@
 
     public static boolean isWearingSafeArmor(LivingEntity p_371788_) {
         for (EquipmentSlot equipmentslot : EquipmentSlotGroup.ARMOR) {
-            if (p_371788_.getItemBySlot(equipmentslot).is(ItemTags.PIGLIN_SAFE_ARMOR)) {
+            if (p_371788_.getItemBySlot(equipmentslot).makesPiglinsNeutral(p_371788_)) {
                 return true;
             }
         }
@@ -792,7 +_,7 @@
     }
 
     private static boolean hasCrossbow(LivingEntity p_34919_) {
-        return p_34919_.isHolding(Items.CROSSBOW);
+        return p_34919_.isHolding(is -> is.getItem() instanceof net.minecraft.world.item.CrossbowItem);
     }
 
     private static void admireGoldItem(LivingEntity p_34939_) {
