--- a/net/minecraft/client/ToggleKeyMapping.java
+++ b/net/minecraft/client/ToggleKeyMapping.java
@@ -8,47 +_,23 @@
 @OnlyIn(Dist.CLIENT)
 public class ToggleKeyMapping extends KeyMapping {
     private final BooleanSupplier needsToggle;
-    private boolean releasedByScreenWhenDown;
 
     public ToggleKeyMapping(String p_92529_, int p_92530_, String p_92531_, BooleanSupplier p_92532_) {
-        this(p_92529_, InputConstants.Type.KEYSYM, p_92530_, p_92531_, p_92532_);
-    }
-
-    public ToggleKeyMapping(String p_435047_, InputConstants.Type p_434172_, int p_435994_, String p_435953_, BooleanSupplier p_433451_) {
-        super(p_435047_, p_434172_, p_435994_, p_435953_);
-        this.needsToggle = p_433451_;
-    }
-
-    @Override
-    protected boolean shouldSetOnIngameFocus() {
-        return super.shouldSetOnIngameFocus() && !this.needsToggle.getAsBoolean();
+        super(p_92529_, InputConstants.Type.KEYSYM, p_92530_, p_92531_);
+        this.needsToggle = p_92532_;
     }
 
     @Override
     public void setDown(boolean p_92534_) {
         if (this.needsToggle.getAsBoolean()) {
-            if (p_92534_) {
+            if (p_92534_ && isConflictContextAndModifierActive()) {
                 super.setDown(!this.isDown());
             }
         } else {
             super.setDown(p_92534_);
         }
     }
-
-    @Override
-    protected void release() {
-        if (this.isDown() || this.releasedByScreenWhenDown) {
-            this.releasedByScreenWhenDown = true;
-        }
-
-        this.reset();
-    }
-
-    public boolean shouldRestoreStateOnScreenClosed() {
-        boolean flag = this.key.getType() == InputConstants.Type.KEYSYM && this.releasedByScreenWhenDown;
-        this.releasedByScreenWhenDown = false;
-        return flag;
-    }
+    @Override public boolean isDown() { return this.isDown && (isConflictContextAndModifierActive() || needsToggle.getAsBoolean()); }
 
     protected void reset() {
         super.setDown(false);
