--- a/net/minecraft/client/gui/render/state/GuiRenderState.java
+++ b/net/minecraft/client/gui/render/state/GuiRenderState.java
@@ -11,6 +_,7 @@
 import net.minecraft.client.gui.render.state.pip.PictureInPictureRenderState;
 import net.neoforged.api.distmarker.Dist;
 import net.neoforged.api.distmarker.OnlyIn;
+import org.apache.commons.lang3.mutable.MutableInt;
 
 @OnlyIn(Dist.CLIENT)
 public class GuiRenderState {
@@ -47,6 +_,14 @@
         this.current = this.current.up;
     }
 
+    public void down() {
+        if (this.current.down == null) {
+            this.current.down = new GuiRenderState.Node(this.current);
+        }
+
+        this.current = this.current.down;
+    }
+
     public void submitItem(GuiItemRenderState p_416573_) {
         if (this.findAppropriateNode(p_416573_)) {
             this.itemModelIdentities.add(p_416573_.itemStackRenderState().getModelIdentity());
@@ -149,18 +_,20 @@
         return this.itemModelIdentities;
     }
 
-    public void forEachElement(Consumer<GuiElementRenderState> p_436752_, GuiRenderState.TraverseRange p_419645_) {
-        this.traverse(p_436488_ -> {
-            if (p_436488_.elementStates != null || p_436488_.glyphStates != null) {
-                if (p_436488_.elementStates != null) {
-                    for (GuiElementRenderState guielementrenderstate : p_436488_.elementStates) {
-                        p_436752_.accept(guielementrenderstate);
+    public void forEachElement(GuiRenderState.LayeredElementConsumer p_418470_, GuiRenderState.TraverseRange p_419645_) {
+        MutableInt mutableint = new MutableInt(0);
+        this.traverse(p_426878_ -> {
+            if (p_426878_.elementStates != null || p_426878_.glyphStates != null) {
+                int i = mutableint.incrementAndGet();
+                if (p_426878_.elementStates != null) {
+                    for (GuiElementRenderState guielementrenderstate : p_426878_.elementStates) {
+                        p_418470_.accept(guielementrenderstate, i);
                     }
                 }
 
-                if (p_436488_.glyphStates != null) {
-                    for (GuiElementRenderState guielementrenderstate1 : p_436488_.glyphStates) {
-                        p_436752_.accept(guielementrenderstate1);
+                if (p_426878_.glyphStates != null) {
+                    for (GuiElementRenderState guielementrenderstate1 : p_426878_.glyphStates) {
+                        p_418470_.accept(guielementrenderstate1, i);
                     }
                 }
             }
@@ -232,6 +_,10 @@
     }
 
     private void traverse(GuiRenderState.Node p_418468_, Consumer<GuiRenderState.Node> p_418350_) {
+        if (p_418468_.down != null) {
+            this.traverse(p_418468_.down, p_418350_);
+        }
+
         p_418350_.accept(p_418468_);
         if (p_418468_.up != null) {
             this.traverse(p_418468_.up, p_418350_);
@@ -246,11 +_,18 @@
     }
 
     @OnlyIn(Dist.CLIENT)
+    public interface LayeredElementConsumer {
+        void accept(GuiElementRenderState p_418211_, int p_418134_);
+    }
+
+    @OnlyIn(Dist.CLIENT)
     static class Node {
         @Nullable
         public final GuiRenderState.Node parent;
         @Nullable
         public GuiRenderState.Node up;
+        @Nullable
+        public GuiRenderState.Node down;
         @Nullable
         public List<GuiElementRenderState> elementStates;
         @Nullable
